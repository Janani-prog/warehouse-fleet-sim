# CLAUDE.md — Project Memory

Read this file first in any new session before doing anything else. It is the source of truth for scope, decisions already made, and where the project currently stands. Update the "Current Status" section at the end of every work session.

## Project
Anomaly Detection & Collision-Safe Navigation in ML-Driven Autonomous Warehouse Robots — a simulation-only academic capstone. No hardware, no paid services, no external hosting.

## Companion Docs (read these too)
- `PRD.md` — goals, scope tiers, success metrics
- `TECHNICAL_ARCHITECTURE.md` — full system design, components, data flow
- `FRONTEND_SPEC.md` — dashboard design spec + Stitch prompt
- `SECURITY.md` — safety/security scope
- `BACKLOG.md` — phased milestone tickets, split into two review-gated parts. **Build Part 1 (M0–M6) in full first** — it's a complete, demoable system on its own (simulator + classical-vs-learned path planning & traffic avoidance + calibrated forecaster + dashboard). Part 2 (M7–M12) adds RAG, the LLM agent, causal evaluation, and closes the loop. Do not start Part 2 tickets before Part 1's "Review 1 Demo Checklist" passes and the repo is tagged (`git tag review-1`).

## Locked Design Decisions (do not silently revisit these)
1. **Forecasting is classification, not regression.** Predict P(anomaly in next N ticks) as binary/multi-class, not a continuous multi-step time series. This was a deliberate re-scope after an earlier attempt found LSTM regression precision too low to trust downstream.
2. **Agent action triggering is confidence-gated + backstopped.** The LLM agent only acts when calibrated LSTM confidence clears a threshold OR a simple rule-based backstop fires. Never wire raw/uncalibrated model output directly into the action loop.
3. **LLM agent is prompt-based by default (via Ollama), fine-tuning is optional stretch.** Only invest Colab GPU time in LoRA fine-tuning if the prompt-only baseline's accuracy on the labeled action-selection test set is inadequate. If fine-tuning is done, keep it as an ablation comparison, not a replacement — report both.
4. **The LLM never executes actions directly.** All output is structured JSON, parsed and validated against a hard-coded whitelist enum (`REASSIGN_TASK`, `REPLAN_ROUTE`, `THROTTLE_ZONE_TRAFFIC`, `NO_ACTION`) by the Action Executor before touching simulator state. Malformed output → `NO_ACTION`, logged, never crashes.
5. **Scope tiers**: Path Planning and Traffic/Collision Avoidance are P0 (core to the project title). Scheduling (Hungarian vs learned) is P1 — full comparison, but the first thing cut under time pressure. LoRA fine-tuning is also P1.
6. **Evaluation is causal, not observational.** Every claimed improvement must come from paired trials (same seed, intervention ON vs OFF) with a statistical test — never "before/after" framing without pairing.
7. **Everything must run free.** Local CPU for sim/ML/RAG/dashboard. Colab free-tier GPU only if fine-tuning is attempted, then quantize and serve locally via Ollama. No paid API calls anywhere in the core pipeline.

## Repo Structure
```
sim/        - simulator core, telemetry logging
ml/         - scheduling, path planning, traffic policies (classical + learned), LSTM forecaster
rag/        - SOP corpus, retrieval pipeline
agent/      - prompt templates, Ollama client, action executor, (optional) LoRA fine-tune scripts
eval/       - causal evaluation harness, benchmark scripts
dashboard/  - React + FastAPI dashboard
data/       - generated datasets, trial logs (gitignored if large)
docs/       - PRD, architecture, frontend spec, security, backlog (this set of files)
```

## Permission to Deviate From Spec
These docs are the plan, not a cage. If, once something is actually built and you can see real behavior/data/results, you believe a different approach would genuinely be better than what's specified here (in the PRD, architecture doc, or backlog) — you're authorized to say so and propose it. This is expected and welcome, not a violation of the plan.

The bar and process for doing this:
- This applies to **implementation choices below the "Locked Design Decisions" level** (e.g. "actually a gradient-boosted tree beats the LSTM classifier on this data" or "the imitation-learned planner needs a different feature set than planned") — propose and proceed with those directly, just document what changed and why.
- For anything that would **change a Locked Decision itself** (the classification-not-regression framing, confidence-gating, the whitelist-only execution model, prompt-first LLM approach, causal-not-observational evaluation, or the $0 constraint) — flag it explicitly, explain the evidence/reasoning, and get a go-ahead before changing course. These exist because they were the product of a specific earlier failure mode (the forecaster-reliability issue), so they need a real conversation, not a silent swap.
- Either way: **never change course silently.** Every deviation, big or small, gets a short note — either inline in the relevant milestone's ticket in `BACKLOG.md`, or in the "Current Status" log below — stating what was specified, what was done instead, and why. The goal is that a fresh session (or you, months later) can reconstruct the reasoning, not just the current state.
- "I can do better" should be backed by something concrete you actually observed while building (a benchmark result, a failure mode, a constraint that turned out to bind harder than expected) — not a preference expressed before anything's been tried.

## Version Control
- **Prerequisite (you do this once, not Claude Code)**: create an empty GitHub repo and either set it as the local `origin` remote, or have the `gh` CLI authenticated (`gh auth login`) so Claude Code can create/push it. Claude Code should not attempt to invent credentials or silently create accounts — if push fails due to auth, it should stop and tell you rather than working around it.
- **Commit per logical unit of work**, not per milestone-and-a-half — e.g. "A* implementation + tests", then separately "learned planner + benchmark script", not one giant commit for all of M2. Commit messages: short imperative summary line, optional body explaining *why* for anything non-obvious (especially any deviation logged per the Permission to Deviate section).
- **Push regularly**, at minimum at the end of every milestone, ideally after each meaningful commit, so the GitHub history is a real record of progress, not a single end-of-session dump.
- **Tag part boundaries**: `git tag review-1` at the end of M6 (Part 1 wrap-up), `git tag review-2` at the end of M12. These are the two points a reviewer might actually check out.
- **Branching**: keep it simple — commit to `main` directly for this solo/academic project unless a milestone involves a risky rewrite you want to be able to abandon cleanly, in which case use a short-lived feature branch and merge back once the milestone's "Done when" condition is met.
- **.gitignore**: exclude `data/` outputs that are large/regeneratable (trial logs, generated datasets, model checkpoints) unless small enough to be useful as committed fixtures; exclude any `.env`, `node_modules/`, Python venvs, and Colab-downloaded model weights.
- **Never commit secrets** — there shouldn't be any in this project's core pipeline (see SECURITY.md §4), but if any local `.env` is ever introduced, it stays gitignored.
- After pushing, note the commit/tag in the "Current Status" section below so a fresh session knows exactly what's already on GitHub.

## Version Control
- Use git from the start (M0). Commit at meaningful checkpoints — at minimum once per completed ticket, not just once per milestone — with clear, descriptive messages (what changed and why, not just "update").
- Author identity: use whatever `git config user.name` / `user.email` is already set locally (i.e. the repo owner's own identity). Do not add, change, or override author/committer identity for any commit.
- **No AI-tool attribution anywhere in the repo.** No `Co-authored-by` trailers, no "Generated with Claude" / "Built with Claude Code" notices, no mentions of Claude or AI assistance in commit messages, code comments, README, or docs. Commits and the repo as a whole should read as ordinary authored work.
- Push to the GitHub remote regularly — at least after every completed milestone, ideally after every completed ticket. If no remote is configured yet, ask for the GitHub repo URL (or create one via `gh repo create` if the `gh` CLI is authenticated) before the first push.
- Tag part boundaries: `git tag review-1` at the end of M6 (Part 1 wrap-up), `git tag review-2` at the end of M12 (final writeup/polish).
- Use a `.gitignore` covering the usual (`__pycache__/`, `node_modules/`, `.env`, large generated `data/` artifacts, model checkpoints unless intentionally versioned).
- Normal branch hygiene is fine but not required — direct commits to `main` per milestone are acceptable for a solo project; use feature branches only if it genuinely helps you avoid a broken `main` mid-milestone.

## Working Agreement for Claude Code Sessions
- Work through `BACKLOG.md` milestones in order (M0 → M10). Don't start a later milestone's tickets before the current one's "Done when" condition is met.
- After finishing a milestone, write a short entry in "Current Status" below (what's done, what's next, any open issue) before ending the session — this is what makes a fresh chat able to resume without re-deriving context.
- If a design decision from the "Locked Design Decisions" list needs to change, say so explicitly and update this file — don't just quietly deviate.
- Prefer simple, testable implementations over clever ones — every classical algorithm (A*, ORCA, Hungarian) needs unit tests before its learned counterpart is built, since the classical version is both the baseline and the ground-truth generator for imitation learning.
- Every run that produces a benchmark or eval result should be reproducible from a single script/command with a fixed seed.

## Current Status
_(update this section each session)_

- **Last updated**: 2026-08-18
- **Current part**: Part 1 (Review 1 target) — **complete, tagged `review-1`**
- **Current milestone**: M6 — complete
- **Completed**:
  - Repo skeleton (`sim/`, `ml/`, `rag/`, `agent/`, `eval/`, `dashboard/`, `data/`, `docs/`) created; source docs (PRD, TECHNICAL_ARCHITECTURE, FRONTEND_SPEC, SECURITY, BACKLOG) moved into `docs/` under canonical filenames.
  - The Stitch design export (5 dashboard screens: fleet map, anomaly timeline, agent action log, causal eval report, loading state — HTML/CSS mockups + screenshots + a full design-token spec) extracted to `docs/design/stitch_warehouse_fleet_operations_console/`. Use this as the concrete visual reference for M5 so the dashboard looks like a finished internal tool, not a placeholder.
  - Python env: `.venv` + `requirements.txt` (numpy, scipy, pandas, pyarrow, torch CPU, scikit-learn, matplotlib, fastapi, uvicorn, pydantic, pytest), installs clean via `--extra-index-url https://download.pytorch.org/whl/cpu` (keeps torch to the ~205MB CPU wheel instead of the much larger CUDA build).
  - **Deviation (below Locked Decisions, not requiring sign-off)**: RAG/agent-only deps (`sentence-transformers`, `rank_bm25`, `faiss-cpu`, `peft`, `transformers`, an Ollama client) are deliberately *not* in `requirements.txt` yet — they're unused until M7/M8 (Part 2), so adding them now would just slow every `pip install` for no benefit. Add them when M7 starts.
  - `pyproject.toml` configures pytest (`pythonpath = ["."]`, testpaths under `sim/ml/eval` `tests/`); one placeholder test passes, confirming the harness is wired.
  - Dashboard scaffolded with Vite + React + TypeScript + Tailwind v4 (via the official `@tailwindcss/vite` plugin, not the old `tailwind init`/PostCSS flow, since v4 dropped that). Vite's demo boilerplate (counter, logos, marketing copy) stripped down to an empty placeholder page — real views land in M5 against the Stitch mockups.
  - Git initialized, default branch renamed `master` → `main`. GitHub repo created via authenticated `gh` CLI: **https://github.com/Janani-prog/warehouse-fleet-sim** (public), set as `origin`. Three logical commits (docs/design, Python skeleton, dashboard scaffold) pushed to `main`.
  - Verified both from clean: `pip install -r requirements.txt` succeeds, `npm install` succeeds, `npx vite build` succeeds, `pytest` passes.

- **M1 — Simulator Core**:
  - `sim/grid.py`: fixed, deterministic 24×16 warehouse layout — 3 horizontal rack bands with vertical cross-aisle gaps, 16 zones (6×4 cells each) for aggregate metrics, 5 dock points on the left edge doubling as robot spawn points and order drop-offs, pickup points on the aisle cells bordering each rack band.
  - `sim/entities.py`: `Robot` (position, state, current order, path stub for M2) and `Order` (origin/destination, lifecycle status, arrival/assign/pickup/dropoff ticks, `wait_ticks`).
  - `sim/order_generator.py`: Poisson-arrival order generator driven by the world's single shared `np.random.Generator`, seeded once — this is what makes runs deterministic end to end.
  - `sim/policies.py`: **M1 placeholder movement only** — greedy random walk biased 80% toward the current target cell, no real pathing or collision avoidance. This is intentionally dumb; it's replaced by A*/learned planning in M2 and ORCA/learned avoidance in M3. Nearest-idle-robot order assignment (Manhattan distance) is likewise a placeholder for Hungarian/learned scheduling (M11, P1).
  - `sim/telemetry.py`: per-tick Parquet tables — `ticks` (min pairwise distance, near-miss count, active orders), `zones` (queue depth + robot density per zone), `robots` (full per-tick position/state for replay), `events` (order arrived/assigned/picked-up/completed), plus `orders.parquet` (full lifecycle) and `manifest.json` (seed/config). This schema is designed to directly serve both M4 (windowed features per tick) and M5 (fleet-map replay) without rework.
  - `sim/run.py`: CLI (`python -m sim.run --seed --ticks --robots --order-rate --out`) — done-when condition verified: a 500-tick, 6-robot, seed-0 run produces a full replayable telemetry log in `data/runs/demo/` (gitignored, regeneratable).
  - Tests (`sim/tests/`): 9 passing — zone/layout sanity, spawn/dock/pickup points free, no-rack-neighbors; same-seed runs produce byte-identical robot trajectories and order histories, different seeds diverge; no robot or order position ever lands out of bounds or inside a rack across a run.
  - **Known tuning issue, not a bug**: with the M1 placeholder random-walk policy, default demo params (6 robots, order_rate=0.4) let the pending-order backlog grow largely unbounded over 500 ticks (mean wait ~184 ticks, only ~20% of orders completed) — the random walk is just too inefficient to keep up. Expected and fine for M1 (whose job is only to prove determinism/bounds/telemetry/replay work), but the default scenario params will need retuning once A* (M2) and real avoidance (M3) land, so the Review-1 demo run shows a fleet that's actually keeping up with load rather than perpetually backlogged.
- **Next up**: M2 — Path Planning. A* implementation + unit tests, A*-solved dataset generation via the simulator, imitation-learned planner trained on that dataset, benchmark script (path length ratio, latency, success rate) comparing classical vs. learned. Swap `sim/policies.py`'s random walk for real A* pathing once it exists.

- **M2 — Path Planning**:
  - `ml/astar.py`: standard 4-connectivity, unit-cost A* with Manhattan heuristic. 6 tests in `ml/tests/test_astar.py` including known-optimal-length cases (open grid, forced detour through a single gap) and real-layout pickup→dropoff routing.
  - `ml/generate_planning_dataset.py`: samples realistic trips (pickup↔dropoff and general free-cell pairs) and solves each with A*, saved to Parquet. Train set (seed 0, n=500) and a separately-seeded held-out test set (seed 999, n=150) in `data/datasets/` (gitignored, regenerate via the script).
  - Learned planner: local-observation imitation policy, not a global planner. `ml/planner_features.py` encodes state as a flattened 5×5 occupancy window around the current cell + normalized goal delta (27-dim) — deliberately small/local per the architecture doc's CPU-tractability guidance. `ml/planner_model.py` is a 2-hidden-layer MLP (64 units) predicting one of 4 moves. `ml/train_planner.py` trains it via supervised cross-entropy against A*'s chosen action at each step of every training path (30 epochs, reached ~97% held-out per-step action accuracy). `ml/learned_planner.py` does the autoregressive rollout (repeated single-step queries, since it has no global search) with a revisit-limited fallback so it can't infinite-loop between two cells.
  - `ml/benchmark_planning.py`: classical vs. learned on the 150 held-out trips, reproducible from a fixed seed. Result: **both 100% success rate**; learned paths average **7.1% longer** than A*'s optimal (well within the PRD's "matches within X%" bar) but A* is faster in wall-clock (~0.24ms vs ~3.4ms per query — the MLP forward-pass overhead exceeds heapq A*'s cost at this grid's tiny size, so the "learned wins on compute time" alternative-axis framing from the PRD does *not* hold here and I'm reporting that honestly rather than reframing the metric). Table: `data/results/planning_benchmark.csv`; chart: `data/results/planning_benchmark.png`.
  - **Wired A* into the live simulator** (`sim/world.py`): robots now cache a full A* path when their target changes (order assigned → pickup; picked up → dropoff) and walk it one cell/tick, replanning only on target change — not the learned planner, which stays a benchmark-only artifact for now (`sim.policies.PlannerFn` is a swappable interface if a learned-driven sim run is wanted later). Idle robots hold position instead of wandering (was M1's random-walk default) — deliberate, for a calmer/more legible fleet-map demo.
  - **Bug found and fixed during integration**: robots whose cached A* paths crossed could wait on each other's occupied cell forever — a genuine permanent deadlock, reproduced with as few as 2 robots, not just a throughput/tuning issue. Fixed with a `stuck_ticks` counter per robot (`sim/entities.py`) and a minimal liveness guarantee in `sim/world.py`: after `STUCK_THRESHOLD=3` blocked ticks, a robot force-side-steps onto any free unclaimed neighbor cell and its path is dropped for a fresh replan next tick (logged as a `deadlock_broken` telemetry event for auditability). This is explicitly a stopgap, not real avoidance — M3's ORCA/learned traffic policy is the principled fix. Regression tests in `sim/tests/test_deadlock.py`.
  - **Default demo params retuned** (`World.__init__`, `sim/run.py` CLI): 6 robots/order_rate=0.4 (M1's placeholder values) left the queue growing unboundedly even with real A* pathing, because fleet throughput capacity was below arrival rate — this surfaced the deadlock bug too, since it's far more visible under dense traffic. Swept a few (robots, order_rate) combinations post-fix; **8 robots / order_rate=0.15** keeps the backlog stable (~92-96% order completion, active-order count settling rather than climbing) and is now the default. Documented here rather than in the backlog since it's a parameter choice, not a scope change.
  - All 26 tests pass (`sim/`: 17, `ml/`: 9). `data/datasets/`, `data/models/`, `data/results/`, `data/runs/` are all gitignored/regeneratable — rerun `ml/generate_planning_dataset.py` → `ml/train_planner.py` → `ml/benchmark_planning.py` → `sim/run.py` in that order from a clean clone to reproduce everything.
- **Next up**: M3 — Traffic/Collision Avoidance. ORCA (or simplified velocity-obstacle) implementation + unit tests, learned local avoidance policy (imitation from ORCA or independent-Q RL), benchmark (near-miss/collision count, throughput, mean detour) classical vs. learned. This directly replaces the M2 stopgap deadlock-breaker with principled multi-robot coordination — expect `deadlock_broken` event counts to become a "before" baseline the M3 benchmark can meaningfully compare against.

- **M3 — Traffic/Collision Avoidance**:
  - **Deviation from literal architecture doc wording, within the backlog's explicitly permitted scope**: the sim is grid/discrete, not continuous-space, so literal ORCA (which reasons about continuous velocity half-planes) doesn't map on directly. Built the "simplified velocity-obstacle variant" the backlog explicitly allows instead: `ml/traffic.py`'s `arbitrate()` is a deterministic, priority-based (lower robot id wins) resolver for vertex conflicts (two robots want the same cell) and edge/swap conflicts (two robots want to trade cells head-on) — the discrete analogue of reciprocal velocity obstacles. 3+-robot rotations through a cycle are intentionally allowed (standard MAPF convention: no two robots ever share a cell at a tick boundary, so it's not a real collision even though a naive continuous-time reading might suggest agents "pass through" each other). 7 unit tests including the backlog's explicitly-named two-robot head-on case.
  - **Design correction made mid-milestone, documented rather than silently fixed**: the first version of the resolver gave a losing robot one "opportunistic side-step" to any free neighbor, chosen goal-blind (lexicographically smallest). This looked reasonable in isolation and passed unit tests, but at the warehouse's single-cell aisle gaps (only 3 crossing points between the 3 rack bands) it caused robots to shuffle sideways and trigger a full path replan on every step instead of just waiting their turn — a self-inflicted jam that dropped order completion from ~93% to ~34% in an 8-robot/600-tick run. Diagnosed via direct desired-vs-final position tracing (a 2-robot low-density run showed zero spurious conflicts, isolating the bug to bottleneck contention specifically). Fix: removed the side-step; a losing robot now simply yields (waits) for the tick, which is itself a legitimate ORCA-style response (velocity 0 avoids the conflict same as dodging does). Verified completion rate recovered to ~82-94% across several (robots, order_rate) configs.
  - **`STUCK_THRESHOLD` repurposed** (was M2's random-jump deadlock-breaker, now goal-aware): after 3 consecutive blocked ticks, `sim/world.py`'s `_replan_around_robots` calls the *same planner* (`self.planner`, works for either A* or a learned planner) against a temporary `Warehouse` copy with every other robot's current cell added to `racks` — a real A* query around the blockage (handles e.g. a persistently parked idle robot sitting in a corridor) instead of a blind random guess. Reuses `dataclasses.replace` on the frozen `Warehouse`, no new obstacle-aware algorithm needed.
  - **Learned avoidance** (`ml/traffic_features.py`, `ml/traffic_model.py`, `ml/learned_traffic.py`): since the classical resolver only ever chooses proceed-or-wait (no side-step anymore), the learned policy is a binary MLP classifier (29-dim input: 5×5 local window distinguishing free/rack/*other-robot* cells + one-hot desired direction → proceed/yield). Trained via imitation of the classical resolver's actual decisions, captured by running full simulator episodes with a new `World(on_move=...)` observation hook (`ml/generate_traffic_dataset.py`) — 42.6k labeled examples from 12 episodes, 95.6% held-out per-decision accuracy (`ml/train_traffic.py`).
  - **Safety-net pattern, matching the project's whitelist-executor philosophy applied one layer down**: a per-robot binary classifier has no way to guarantee *joint* collision-freedom across robots, so `ml/learned_traffic.py`'s `make_learned_avoidance` never lets model predictions touch simulator state directly — it filters robots down to those predicted "proceed," then routes the result through the *same* `ml.traffic.arbitrate()` safety net the classical policy uses. `ml/tests/test_learned_traffic.py` proves this with an adversarial model that always predicts "proceed" on a head-on swap and confirms no collision slips through regardless.
  - **Benchmark** (`ml/benchmark_traffic.py`): 20 held-out seeds (2000-2019), 10 robots, order_rate 0.2, 400 ticks each, classical vs. learned run on identical seeds (paired). Wilcoxon signed-rank on the paired deltas: **completion rate is statistically indistinguishable** (p=0.21, learned +0.5pp) — **near-miss count is significantly lower under learned** (p=0.0027, -577/run, ~-22.5%) — **mean detour is significantly higher under learned** (p=0.0023, +3.1 ticks/order). Read this as a genuine, reportable tradeoff (fewer close calls, slightly less efficient routes), not a straightforward win or loss for either policy — worth highlighting in the writeup as exactly the kind of "net benefit under imperfect precision" analysis the PRD calls for. Table: `data/results/traffic_benchmark.csv`; paired stats: `data/results/traffic_benchmark_paired_stats.csv`; chart: `data/results/traffic_benchmark.png`.
  - All 44 tests pass (`sim/`: 17, `ml/`: 27). Reproduce end to end: `ml.generate_traffic_dataset` → `ml.train_traffic` → `ml.benchmark_traffic`.
- **Next up**: M4 — Anomaly Forecaster. Label congestion/collision-risk anomaly events from telemetry rules, generate a windowed dataset from many simulated episodes, train the LSTM classifier (binary/multi-class, CPU), calibrate (temperature scaling or isotonic regression) on a held-out split, implement the independent rule-based backstop, report precision/recall at the chosen operating threshold + calibration curve. The `near_miss_count`/`replans_around_blockage`/`active_orders` telemetry from M1-M3 is exactly the signal this milestone needs to label and predict from — no new telemetry fields should be required.

- **M4 — Anomaly Forecaster**:
  - **Ground-truth labels** (`ml/anomaly_labels.py`): congestion = `active_orders >= 40`, collision-risk = `near_miss_count >= 6` in a tick — both thresholds chosen from the observed distribution across a deliberate mix of healthy and overloaded (robots, order_rate) configs (not guessed), landing around the 70th/80th percentile respectively so there's real positive-class signal to learn from.
  - **Windowed dataset** (`ml/generate_forecast_dataset.py`): 6-dim per-tick feature vector (active_orders, near_miss_count, max/mean zone queue depth, max zone density, count of blocked robots — all already-existing telemetry, no new instrumentation) over a rolling 30-tick window, labeled by whether each anomaly type occurs in the *next* 10 ticks (the classification-not-regression framing from Locked Decision #1). Generated from 9 different (robots, order_rate) configs × several seeds each (36 episodes, 23.8k windows train / 18 episodes, 11.9k windows held-out test) specifically so both calm and overloaded dynamics are represented — the tuned "healthy demo" config alone rarely produces a real anomaly.
  - **Model** (`ml/forecaster_model.py`): 1-layer, 32-hidden-unit LSTM → 2 independent binary logit heads (congestion, collision), per the architecture doc's CPU-tractability guidance. Trains in well under a minute on CPU.
  - **Calibration methodology change made mid-milestone, documented rather than silently swapped**: started with isotonic regression (`ml/calibration.py`'s first version). It calibrated well in the aggregate sense but its piecewise-constant fit collapsed a wide range of raw probabilities to a single output (usually 0) — turning what should be a gradually-rising confidence signal within one trajectory into a near step function. Diagnosed concretely: the scripted congestion-spike demo (see below) showed calibrated confidence flat at 0.0 from tick 195 to 254, then jumping straight to 0.43+, while the *raw* (pre-calibration) sigmoid output was visibly climbing from ~tick 233 onward. Switched to temperature scaling (single learned scalar T per head, fit via LBFGS minimizing validation NLL) — both are explicitly named as acceptable alternatives in the architecture doc, so this is an implementation-level swap, not a Locked Decision change. Also had to raise `TARGET_PRECISION` from 0.75 → 0.9 after the swap: the model turns out to be extremely confident once a real trend is present (congestion is close to a deterministic function of the current active-orders trajectory), so its calibrated probability crosses most of the achievable precision/recall range over a very narrow probability band — 0.9 was chosen by directly inspecting the precision-recall curve, landing in the smooth region.
  - **Held-out test-set report** (`data/models/forecaster/report.json`, never touched during training/calibration): congestion — precision 0.856, recall 0.996, F1 0.921 at threshold 0.00071 (post-temperature-scaling). collision-risk — precision 0.914, recall 0.669, F1 0.773 at threshold 0.805. Collision-risk is the harder target (near-misses are noisier/more stochastic than the highly autocorrelated order backlog) — reported plainly, not smoothed over. Calibration curves (raw vs. calibrated vs. perfect) at `data/models/forecaster/calibration_curves.png`.
  - **Rule-based backstop** (`ml/rule_backstop.py`): same thresholds as the ground-truth labels, checked against the *current* tick only — genuinely independent of the LSTM (no shared imports with the model/calibration stack, asserted directly in a test), so the system degrades gracefully if the classifier is ever wrong or unavailable.
  - **Live inference** (`ml/forecaster.py`): a `Forecaster` class wrapping rolling-window buffering + calibrated LSTM inference + the backstop into one `.step(features) -> dict` call per tick, returning calibrated confidence *and* raw confidence per head (the latter purely for trend visibility, e.g. on the dashboard — never used for the trigger decision) plus both trigger booleans and their OR. This is the trustworthy signal Part 2's agent will consume later (Locked Decision #2) — nothing acts on it yet in Part 1, it just needs to exist and be logged.
  - **Scripted congestion-spike scenario** (`ml/scripted_scenarios.py`, `ml/run_forecast_demo.py`): `OrderGenerator.arrival_rate` now accepts a callable, not just a constant, so a scenario can vary load over a run. Baseline rate 0.15, spiked to 0.6 for ticks 200-260 on an 8-robot fleet. Result on seed 0: actual congestion event (active_orders ≥ 40) first hits at **tick 251**; calibrated congestion-head confidence crosses its threshold at **tick 235** — a genuine **16-tick early warning**, satisfying the Review 1 Demo Checklist's "flagging risk before it happens... with calibrated confidence shown" requirement. Run output saved to `data/runs/congestion_spike_demo/` including a `forecast.parquet` telemetry stream (new `TelemetryLogger.log_forecast`) for the dashboard to plot in M5.
  - **Done-when verified**: classifier and backstop both produce visible trigger signals with confidence values on a live run (`ml.run_forecast_demo`'s console output + the saved `forecast.parquet`).
  - All 67 tests pass (`sim/`: 17, `ml/`: 50). Reproduce end to end: `ml.generate_forecast_dataset` (train + test, different seeds) → `ml.train_forecaster` → `ml.run_forecast_demo`.
- **Next up**: M5 — Dashboard, Part 1 scope. FastAPI backend serving telemetry/log data from local storage; Fleet Map, KPI strip, and Anomaly Timeline views only (Agent Action Log and Causal Eval Report are Part 2). Wire to a completed run — the `congestion_spike_demo` run from M4 is a good candidate since it has real forecast data to show on the Anomaly Timeline, not just flat zeros. Use `docs/design/stitch_warehouse_fleet_operations_console/` (the Stitch export from M0) as the concrete visual reference — it already has HTML/CSS mockups + screenshots for exactly these views plus the full design-token spec (Axiom Operations: `#0066FF` accent, Inter + JetBrains Mono, 4px grid, flat/no-shadow). This is the milestone that makes the project *look* like a finished tool for Review 1, not just work correctly.

- **M5 — Dashboard, Part 1 scope**:
  - **Backend** (`dashboard/backend/main.py`): FastAPI app reading directly from each run's Parquet files under `data/runs/<run_id>/` — no DB, static replay only per scope. Endpoints: `/api/runs` (list with manifest), `/api/runs/{id}/manifest|robots|zones|orders|ticks|forecast|events|kpis`, `/api/warehouse` (fixed layout, not run-scoped), and `/api/forecaster/thresholds` (reads the *actual trained* calibrated thresholds from `data/models/forecaster/thresholds.json` — the Anomaly Timeline draws its reference line from this, not a guessed constant, so it stays correct if the model is retrained). `/api/runs/{id}/kpis` computes throughput, completion rate, and mean wait server-side from `orders.parquet`/`ticks.parquet`/`manifest.json` rather than duplicating that logic in the frontend. 9 backend tests (`dashboard/backend/tests/test_main.py`) covering missing-run 404s, empty-file edge cases, and the new thresholds endpoint.
  - **Frontend**: Vite/React/TS, styled directly from the Stitch export's Axiom Operations design tokens (`dashboard/src/index.css` `@theme` block — exact hex values, Inter/JetBrains Mono via `@fontsource` so it's fully offline-safe, no Google Fonts CDN call). `dashboard/src/lib/api.ts` is the single typed fetch client. Layout: fixed `Sidebar` (nav across all 5 eventual views, Part-2 ones shown disabled with a "Part 2" tag rather than hidden, so the full IA is visible now) + `TopBar` (run picker, correction-loop status stub) + a content area routed by `App.tsx`'s local `view` state (no router needed for 5 tabs).
  - **Fleet Map** (`views/FleetMapView.tsx`): animated SVG replay of `robots.parquet` — play/pause + scrubber over ticks, robots colored by state (idle/moving/blocked), pending/assigned order-origin pins, plus a live 4-card KPI strip that updates per-tick (active robot count, blocked count) alongside the run-level KPIs.
  - **KPIs** (`views/KpisView.tsx`): static run-summary grid from `/api/runs/{id}/kpis`.
  - **Anomaly Timeline** (`views/AnomalyTimelineView.tsx`): recharts line chart of calibrated congestion/collision confidence vs. tick, with a dashed `ReferenceLine` at the real trained threshold and a trigger scatter row underneath. Handles the case where a run has no `forecast.parquet` (pre-M4 runs) with an explanatory empty state pointing at `congestion_spike_demo`.
  - **Agent Action Log / Causal Eval Report**: `ComingSoonView` placeholder, explicitly not real views yet — correctly out of scope until M10.
  - **Verified**: `tsc --noEmit` and `vite build` both clean; `pytest dashboard/backend/tests/` 9/9 pass; full 76-test suite (`sim`+`ml`+`dashboard`) passes; manually loaded the dashboard in-browser against `congestion_spike_demo` — all three Part-1 views render correctly with live data (animation plays, KPI cards populate, confidence chart shows the actual congestion spike crossing the trained threshold), zero console errors.
  - **Done-when verified** per `BACKLOG.md` M5: dashboard loads a real run's data and all three Part-1 views render correctly and look presentable.
- **M6 — Part 1 Wrap-up**:
  - **Review 1 Demo Checklist run through end to end**: live replayable simulation with a moving/order-completing fleet (Fleet Map, verified in-browser) ✓; classical-vs-learned comparison numbers for both path planning and traffic avoidance exist and are reproducible (`data/results/planning_benchmark.csv`, `data/results/traffic_benchmark.csv`) ✓; forecaster visibly flags the scripted congestion spike 16 ticks early with calibrated confidence shown on the Anomaly Timeline ✓; dashboard shows fleet map + KPI strip + anomaly timeline, polished (Axiom Operations design tokens, not a debug view) ✓; README written ✓.
  - Root `README.md` rewritten: what's-built summary with the actual headline numbers pulled from each milestone's benchmark (not restated from memory — cross-checked against `data/results/` and `data/models/forecaster/report.json`), the "observes and predicts, does not yet act autonomously" framing the checklist requires, full setup instructions for both the Python side and the dashboard (frontend + backend, since the backend needs a separate `uvicorn` command not previously documented anywhere), and the exact seeded command sequence to regenerate every dataset/model/benchmark/run from a clean clone.
  - **Tagged `review-1`** at this commit — Part 1 is feature-complete and demoable on its own; Part 2 (M7+) starts from here.
- **Next up**: M7 — RAG Pipeline (Part 2 start). Author synthetic SOP corpus (15-30 docs covering anomaly types × severities), build BM25 + dense embedding hybrid retrieval, hand-label a retrieval eval set. Per the "Permission to Deviate" section, `requirements.txt` still needs the RAG-only deps added now (`sentence-transformers`, `rank_bm25`, `faiss-cpu`) — deliberately deferred until this point per the M0 status note.
- **Open issues / blockers**: —

- **M7 — RAG Pipeline**:
  - **Deviation from the architecture doc's exact dependency list, below the Locked Decisions level, documented per the Permission to Deviate section**: `docs/TECHNICAL_ARCHITECTURE.md` §2.6 named `sentence-transformers` for local dense embeddings and `faiss-cpu` as an optional vector store. Found while building this: local Ollama (`ollama serve`, already required for M8's agent) already had `nomic-embed-text` pulled and its `/api/embeddings` endpoint works over plain `httpx` (already a Part-1 dependency) — so dense embeddings go through Ollama instead of adding `sentence-transformers` (which would pull in `transformers`/a second model-loading stack purely for embeddings, when the project already needs Ollama running for M8 anyway). `faiss-cpu` was skipped as originally planned — the architecture doc itself calls it "optional overkill" at this corpus size, and `rag/dense_index.py` does plain in-memory cosine search over a cached embedding matrix (cached to `data/cache/rag_dense_embeddings.npz`, keyed by a hash of corpus content so a corpus edit auto-invalidates it). Only new pip dependency added: `rank_bm25==0.2.2`. `peft`/`transformers` stay deferred unless LoRA fine-tuning (M8's P1 stretch) is actually pursued. RAG retrieval is still genuinely hybrid (BM25 + dense embedding similarity, RRF-fused) — this changes *how* dense embeddings are computed, not the retrieval architecture itself.
  - **Corpus** (`rag/data/sop_corpus.json`, loaded via `rag/corpus.py`): 18 synthetic SOP docs. 8 core docs = the 2 anomaly types the M4 forecaster predicts (`congestion`, `collision_risk`) × 4 severity bands (`low`/`moderate`/`high`/`critical` — critical means the M4 rule-based backstop is firing *this tick*, high means the classifier crossed its calibrated threshold ahead of the backstop, moderate/low are sub-threshold monitoring bands with no action recommended). Plus 4 zone-specific variants (dock-saturation congestion → `REASSIGN_TASK` instead of throttling, cross-aisle-gap congestion → `REPLAN_ROUTE`, cross-aisle-gap collision risk → `THROTTLE_ZONE_TRAFFIC`, idle-robot-blocking-a-corridor collision risk → `REASSIGN_TASK`) specifically so retrieval has to distinguish same-type-same-severity docs by content, not just metadata. Plus 6 general/cross-cutting docs (false-positive handling, concurrent multi-anomaly priority, backstop-vs-classifier precedence, human-operator escalation, a severity-band reference doc, post-incident review) that double as retrieval decoys. Every doc's `recommended_action` is validated in tests against the canonical whitelist enum (see below), so the corpus can't silently drift from Locked Decision #4.
  - **Whitelist enum formalized** (`agent/action_schema.py`): `Action` enum + `WHITELISTED_ACTIONS` frozenset — `REASSIGN_TASK`, `REPLAN_ROUTE`, `THROTTLE_ZONE_TRAFFIC`, `NO_ACTION`, exactly Locked Decision #4. Built now (a milestone early) because the RAG corpus needed something concrete to validate `recommended_action` against; M8 imports this same module rather than redefining it.
  - **Retrieval pipeline**: `rag/bm25_index.py` (rank_bm25 `BM25Okapi`), `rag/dense_index.py` (Ollama `nomic-embed-text` + cosine similarity), `rag/hybrid_retrieval.py` (Reciprocal Rank Fusion, k=60 standard damping constant — chosen over a weighted score blend because BM25 scores and cosine similarities aren't on comparable scales, so fusing on rank position sidesteps that). `rag/query_builder.py` turns one forecaster head's signal (calibrated prob, classifier-triggered bool, per-head backstop bool, optional zone context) into a severity band + natural-language retrieval query — built now since M8's agent orchestrator will call straight into it; per-head backstop is computed via `ml.anomaly_labels.is_congestion_event`/`is_collision_risk_event` directly rather than `ml.forecaster.Forecaster.step()`'s single combined `backstop_triggered` bool, which is an OR of both heads and isn't specific enough to build a per-head query from.
  - **Retrieval eval** (`rag/data/retrieval_eval_set.json`, `rag/benchmark_retrieval.py`): 18 hand-labeled queries, one per corpus doc — mix of queries shaped like `query_builder`'s programmatic output (e.g. "Congestion risk, critical severity. Calibrated confidence 0.85...") and free-form natural-language operator phrasing (e.g. "Near-misses keep happening at the same single-cell aisle gap...") so the eval isn't just testing string-similarity to the query template. Benchmarked BM25-only, dense-only, and hybrid RRF side by side against a target of top-1 ≥ 0.75. **Honest result, not the expected one**: hybrid RRF hits top-1 0.778 / top-3 0.889 — meets target — but **dense-only alone scores equal-or-better** (top-1 0.778, top-3 0.944); BM25-only trails on both (top-1 0.611, top-3 0.833). At this corpus size (18 docs, short unambiguous eval queries), the embedding model's semantic matching alone is already strong enough that RRF fusion doesn't add measurable lift and even costs a bit on top-3 recall. Reported as-is rather than reframed — worth flagging in the final writeup as a case where a real result diverged from the "hybrid should always help" assumption. `data/results/retrieval_benchmark.csv` + `retrieval_benchmark_report.json`.
  - Embeddings are cached (`data/cache/rag_dense_embeddings.npz`) so re-running tests/eval doesn't re-hit Ollama for the same 18 static docs every time.
  - 19 new tests (`rag/tests/`: corpus validity + whitelist-membership, BM25 keyword relevance, query-builder severity-band logic, hybrid/dense retrieval — the latter two skip cleanly if `ollama serve` isn't running rather than failing the whole suite, since Ollama is an external local service). Full suite: **95 passed** (`sim`: 17, `ml`: 50, `dashboard`: 9, `rag`: 19).
  - `pyproject.toml` testpaths extended to include `rag/tests` and `agent/tests`.
- **Next up**: M8 — LLM Agent + Action Executor. Prompt-based agent via Ollama (`llama3.1:8b`, already pulled locally) producing structured JSON constrained to `agent/action_schema.py`'s whitelist; `agent/executor.py` validates and never lets raw output touch simulator state; labeled (anomaly, SOP, correct action) eval set for agent accuracy; wire the full closed loop (forecaster/backstop trigger → `rag.query_builder` → `rag.hybrid_retrieval` → agent → executor → simulator). LoRA fine-tuning stays P1/stretch per Locked Decision #3 — only pursue if the prompt-only baseline's eval accuracy is inadequate, and Colab GPU access isn't available in this execution environment regardless, so treat it as explicitly out of scope unless the user provides fine-tuned weights.
- **Open issues / blockers**: —

- **M8 — LLM Agent + Action Executor**:
  - **Real simulator actions added** (`sim/world.py`): the whitelist was previously just an enum with no execution semantics. `World.throttle_zone(zone_id, duration)` defers new order assignment in that zone for `duration` ticks (via `throttled_zones`, checked in `_assign_orders`); `World.reassign_order(order_id)` un-assigns an in-progress (not-yet-picked-up) order back to `PENDING` and frees its robot, to be picked up by nearest-idle-robot assignment next tick; `World.force_replan(robot_id)` clears a robot's cached path so `_move_robots` recomputes from scratch. All three return `bool` (valid target or not), log a telemetry event, and are only ever called by `agent/executor.py`, never directly. Refactored `_log_telemetry`'s per-zone queue/density computation into a public `World.zone_stats()` so `agent/context.py` doesn't duplicate it.
  - **Mid-build correction to the RAG corpus, not just new agent code**: found while writing the agent eval set that `collision-high-idle-robot`'s SOP text recommended `REASSIGN_TASK` ("give the idle robot a new order") for a scenario where the robot is idle and has no in-progress order to reassign — a guaranteed no-op against the real `reassign_order` mechanism above. The simulator's actual self-healing path for this scenario is the *blocked* robots detouring around the stationary obstruction, which is exactly `REPLAN_ROUTE` targeting the blocked robot (matching what `agent/context.py`'s stuck-robot candidate already offers). Corrected the doc's `recommended_action` and text to `REPLAN_ROUTE` rather than leaving a technically-inexecutable SOP in the corpus.
  - **Candidate-scoped action design** (`agent/context.py`): the LLM never invents a zone/robot/order id. Each tick, `build_candidates()` computes at most one concrete candidate per action type from live world state — busiest zone by queue-depth+density, an `ASSIGNED`-not-yet-picked-up order on a robot in that zone (falling back to any such order), and the most-consecutively-blocked robot (if any) — and the prompt (`agent/prompt.py`) offers only these. `agent/executor.py` then requires the LLM's `target_id` to match the offered candidate exactly (string-compared, tolerant of JSON-mode returning ids as strings) *and* re-validates against current world state before dispatch; anything else (malformed JSON, non-whitelisted action, mismatched/invented target, no candidate available for the chosen action) falls back to `NO_ACTION`, logged with a specific error tag, never raises. Proven with an adversarial test (`agent/tests/test_executor.py::test_adversarial_target_id_not_in_candidates_is_rejected`) using a real-but-unoffered zone id.
  - **Prompt** (`agent/prompt.py`): system prompt fixes the 4-action whitelist + required `{"action", "target_id", "reason"}` JSON shape + 3 few-shot examples (including one showing correct NO_ACTION fallback when the SOP's action has no available candidate). `agent/ollama_client.py` calls Ollama's `/api/chat` with `format: "json"` (forces syntactically valid JSON structurally, not enum-constrained - the executor still owns whitelist/candidate validation) and `temperature: 0` for reproducibility.
  - **`ml/forecaster.py` extended** (small, backward-compatible addition): `Forecaster.step()` now also returns `congestion_classifier_triggered` / `collision_classifier_triggered` per-head booleans, alongside the existing combined `classifier_triggered` OR. Needed because `rag/query_builder.py`'s per-head severity classification requires knowing whether *that specific head* crossed its calibrated threshold, not the OR of both heads.
  - **Orchestrator** (`agent/orchestrator.py`): `run_loop_step(world, forecaster_result, retriever, model)` is the concrete implementation of the architecture doc's data-flow diagram for one tick. Per-head backstop uses `ml.anomaly_labels` directly (not `Forecaster.step()`'s combined bool). If both heads reach high/critical severity in the same tick, builds a combined query (collision-risk's confidence used for logging priority, per the `concurrent-multi-anomaly` SOP's stated precedence) instead of picking one head arbitrarily. Retrieves top-1 SOP, builds candidates, calls the LLM, executes, returns a `LoopResult`. An unreachable Ollama server during the chat call is caught and treated identically to malformed LLM output (falls back to `NO_ACTION` via the executor, never raises) - proven in `agent/tests/test_orchestrator.py::test_ollama_unreachable_during_chat_falls_back_to_no_action` via monkeypatching a simulated outage.
  - **Real bug found and fixed via this closed-loop testing, not just unit tests**: naive equal-weight RRF (`rag/hybrid_retrieval.py`, from M7) let the `confidence-threshold-reference` glossary SOP outrank the actually-correct doc on a live orchestrator query, because that doc legitimately repeats every severity band's vocabulary and so wins BM25 rank almost regardless of the real anomaly, even when dense embeddings alone got it right. Switched to *weighted* RRF (`DENSE_WEIGHT=5.0` vs `BM25_WEIGHT=1.0`) - the backlog explicitly allows "RRF or weighted fusion," so this is within M7's original scope, not a new deviation. Re-ran the M7 retrieval benchmark after the change: hybrid now matches dense-only exactly (top-1 0.778, top-3 0.944, both up from the original equal-weight hybrid's 0.778/0.889) - net improvement, not a regression, and `data/results/retrieval_benchmark.csv`/`_report.json` were regenerated.
  - **Agent accuracy eval** (`agent/data/agent_eval_set.json`, `agent/benchmark_agent.py`): 16 hand-labeled (anomaly query, ground-truth SOP, action candidates, correct action) cases - retrieval is held fixed at the correct SOP per case (per the backlog's exact wording), isolating the LLM's action-selection accuracy from M7's already-measured retrieval accuracy. Includes cases where the SOP's recommended action has no matching candidate offered, to test the "don't invent an id, fall back to NO_ACTION" rule specifically. **Result: 16/16 (100%) on `llama3.1:8b` prompt-only**, well above the 0.75 target - `data/results/agent_benchmark.csv`/`_report.json`. Per Locked Decision #3, this result means **LoRA fine-tuning is not pursued**: the prompt-only baseline is clearly adequate, so there's no evidence-based case for spending Colab GPU time on it. (Colab access isn't available in this execution environment regardless, but the accuracy result is the actual reason, not the constraint - if a future session found prompt-only accuracy inadequate on a harder eval set, fine-tuning would need a real conversation per the Permission to Deviate section, since it touches Locked Decision #3.)
  - **Closed-loop demo** (`agent/run_agent_demo.py`): runs M4's scripted congestion-spike scenario with the full loop wired in every triggered tick, logging each decision via a new `TelemetryLogger.log_agent_decision` → `agent_decisions.parquet` (the M10 dashboard's future Agent Action Log data source). A 260-tick smoke test (`data/runs/agent_demo_smoketest/`) produced **35 real closed-loop cycles, first at tick 45, all 35 executed successfully** (25 `NO_ACTION`, 10 `THROTTLE_ZONE_TRAFFIC`) - satisfies M8's done-when condition (a live run demonstrating at least one full trigger→retrieve→decide→act cycle, logged) directly. A fuller 500-tick run (`data/runs/agent_closed_loop_demo/`) was kicked off to give M10's dashboard richer data matching the actual scripted spike window (ticks 200-260) rather than just the smoke test's partial window - CPU-only `llama3.1:8b` inference makes this slow (single-digit-seconds per call, and every triggered tick makes a call), so it was left running in the background rather than block the session; **pick this run up in the M10 session** (check `data/runs/agent_closed_loop_demo/` exists and re-run `python -m agent.run_agent_demo` if not, before wiring the dashboard's Agent Action Log view to it).
  - 41 new tests across `sim/tests/test_agent_actions.py` (9, the new `World` action methods), `agent/tests/` (32: `test_executor.py` 11, `test_context.py` 6, `test_orchestrator.py` 5 - skips cleanly without Ollama - , plus the earlier `test_executor`/`test_context` etc.), `ml/tests/test_forecaster.py` (+1, per-head trigger exposure). Full suite: **127 passed** (`sim`: 20, `ml`: 51, `dashboard`: 9, `rag`: 19, `agent`: 28).
- **Next up**: M9 — Causal Evaluation Harness. Paired-trial runner (same seed, closed-loop ON vs OFF, N ≥ 30 pairs) over the congestion-spike scenario (and/or the default steady-state scenario); collect throughput, mean wait, near-miss count, congestion duration per trial; Wilcoxon signed-rank on paired deltas + effect size + CI; reproducible report from one command. "Loop OFF" should be a straightforward flag on `agent.run_agent_demo`-equivalent logic that skips `run_loop_step` entirely (forecaster/backstop still run and log, just nothing acts on the trigger) so the only real difference between paired trials is whether the action executor ever touches simulator state.
- **Open issues / blockers**: the 500-tick `agent_closed_loop_demo` run was started in the background at the end of the M8 session and its completion wasn't observed before the session moved on - verify it finished cleanly (or re-run it) before M10 wires the dashboard to it.

- **M9 — Causal Evaluation Harness (built)**:
  - **`eval/stats.py`**: `paired_test(deltas)` - Wilcoxon signed-rank (the architecture doc's nonparametric default) plus paired Cohen's d as effect size and a 5000-resample bootstrap 95% CI on the mean delta, since a p-value alone doesn't convey magnitude or uncertainty. `interpret()` turns a result into the plain-language sentence the backlog's "results report... with plain-language interpretation" line calls for. Fully unit-tested (8 tests) without any simulator/Ollama dependency - pure array-in, dict-out functions.
  - **`eval/causal_trial.py`**: `run_trial(seed, ticks, robots, order_rate, forecaster, loop_enabled, retriever=None, agent_model=None)` runs one trial; `loop_enabled=False` never imports/calls anything Ollama-dependent (the forecaster still runs and its output is discarded rather than acted on), so paired trials' OFF half is always fast and free. Metrics: `throughput` (orders reaching `COMPLETED`), `mean_wait` (mean `wait_ticks`, i.e. arrival→pickup delay, over all picked-up orders - deliberately not the same population as `throughput`, matching the architecture doc's "per-order wait time" wording), `near_miss_total` (summed over the trial), `congestion_ticks` (count of ticks where `ml.anomaly_labels.is_congestion_event` is true - the "congestion duration" metric). `forecaster.window` is explicitly cleared at the start of each trial so rolling-window state can't leak from one trial into the next.
  - **Scenario refactor** (`ml/scripted_scenarios.py`): factored the M4 congestion-spike shape into a reusable `make_spike_rate(baseline, spike, start, end)` factory; `congestion_spike_rate` (M4's original 500-tick/spike-at-200 scenario) is now just `make_spike_rate(BASELINE_RATE, SPIKE_RATE, SPIKE_START, SPIKE_END)`, unchanged in behavior. M9 uses the same factory with compressed timing.
  - **Practical constraint found while running the M8 demo, which directly shaped M9's default scenario size**: CPU-only `llama3.1:8b` inference plus repeated dense-embedding calls appears to force Ollama to swap models in memory between a triggered tick's retrieval call and its chat call - a single isolated `chat_json` call measured ~4.4s, but the M8 demo's full 500-tick run (which has many more triggered ticks once the scripted congestion spike takes hold) was still running well over an hour later. Since only loop-ON trials pay any LLM cost at all (loop-OFF is always fast), and N pairs means N ON-trials, `eval/run_causal_eval.py`'s default scenario is deliberately compressed relative to M4's: 140 ticks per trial (not 500), spike at ticks 30-70 (not 200-260) - same shape, much shorter, so an N=30 paired run is tractable instead of potentially taking many hours. Documented here rather than silently shipping a slow default.
  - **`eval/causal_harness.py`**: `run_paired_trials()` runs N pairs (building one shared `Forecaster` + `HybridRetriever` up front, reused - not per-trial - since both are read-only/stateless across trials apart from the explicitly-cleared rolling window); `build_report()` computes the paired stats per metric (`METRICS_LOWER_IS_BETTER` dict controls the "improved" vs "worsened" direction in each interpretation) and drops any metric whose deltas are all-`NaN` for a given run rather than crashing. `run_and_report()` ties it together and writes `causal_eval_trials.csv` + `causal_eval_report.json`. 7 more tests (`eval/tests/test_causal_trial.py`, `test_causal_harness.py`) cover the loop-OFF path (fully Ollama-free, including same-seed reproducibility and window-clearing) and report assembly against a synthetic DataFrame - genuinely fast, no live run needed to verify correctness.
  - **`eval/run_causal_eval.py`**: `python -m eval.run_causal_eval --n-pairs 30 ...` - M9's done-when condition (a reproducible statistical report from a single command).
  - **Not yet run at N=30 in this session** - see Open Issues below; the code path is fully tested and the loop-OFF half needs no Ollama, but a real N=30 run needs the LLM available and uncontended, which wasn't true while the M8 demo was still running in the background.

- **M10 — Dashboard, Part 2 extension (built)**:
  - **Backend** (`dashboard/backend/main.py`): two new endpoints. `/api/runs/{run_id}/agent_decisions` reads `agent_decisions.parquet` (M8's new telemetry table), same graceful-empty-list pattern as every other per-run table for a run that predates M8. `/api/causal_eval` reads `data/results/causal_eval/causal_eval_report.json` - not run-scoped (like `/api/forecaster/thresholds`), since there's one current M9 report, not one per simulator run; 404s until `eval.run_causal_eval` has been run at least once. 4 new backend tests (13 total).
  - **Frontend**: `dashboard/src/views/AgentActionLogView.tsx` - KPI strip (triggered cycles, executed count, throttle-action count) + an expandable table (click a row for the LLM's full reason + raw JSON output), action-type badges, empty state pointing at `agent.run_agent_demo` for a pre-M8 run. `dashboard/src/views/CausalEvalView.tsx` - per-metric cards (mean paired delta, 95% CI, p-value, the plain-language interpretation string from `eval/stats.py` verbatim) with an improved/worsened/not-significant badge, empty state pointing at `eval.run_causal_eval`. Both wired into `Sidebar.tsx` (now un-gated - the `comingSoon` plumbing was removed entirely rather than left dead, since no further Part-2 views are planned) and `App.tsx`; `ComingSoonView.tsx` deleted as now-unused. Sidebar subtitle updated from "Part 1 — Observe & Predict" to "Part 2 — Observe, Decide & Act".
  - **Verified**: `tsc --noEmit` clean, `vite build` clean, full Python suite (146 tests: `sim` 20, `ml` 51, `dashboard` 13, `rag` 19, `agent` 28, `eval` 15) passing. **Not yet manually verified in-browser against real M8/M9 data** (the M8 demo run and the M9 N=30 run were both still pending/running at the point this was built) - do that once both are available, per the project's "test the golden path in a browser, don't just trust the type-checker" standard.
- **Next up**: run the pending `agent.run_agent_demo` 500-tick run and `eval.run_causal_eval` (N=30) to completion, manually verify all 5 dashboard views in-browser against that real data (Fleet Map/KPIs/Anomaly Timeline against `agent_closed_loop_demo`, Agent Action Log against the same run, Causal Evaluation Report against the N=30 run's output), then M6-style wrap: `docs/BACKLOG.md`'s M11 (Hungarian vs. learned scheduling, P1 - cut first under time pressure per the Scope Tiers) if time allows, then M12 (consolidate all benchmark reports into a final writeup, tag `review-2`).
- **Open issues / blockers**: (1) the 500-tick `agent_closed_loop_demo` run from M8 was still running in the background at the start of the M9/M10 session covered above and its completion still wasn't observed - CPU-only LLM inference appears much slower than a single isolated call would suggest, possibly due to Ollama swapping between the embedding model and the chat model in memory on every triggered tick; (2) M9's real N=30 paired run has not been executed yet, deliberately held back to avoid contending with the still-running M8 demo for the same local Ollama process; (3) M10's new dashboard views are code-complete and unit/type/build-tested but not yet manually verified in a browser against real data, for the same reason.
