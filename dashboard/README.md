# Fleet Ops Dashboard

React + Vite + TypeScript + Tailwind, FastAPI backend (added in M5). Read-only
visualization of simulator runs — see `../docs/FRONTEND_SPEC.md` for the design
spec and `../docs/design/` for the Stitch mockups this implements against.

```
npm install
npm run dev
```
