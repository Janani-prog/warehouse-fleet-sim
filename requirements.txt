# Part 1 (M0-M6) dependencies: simulator, classical + learned planning/traffic,
# LSTM forecaster, dashboard backend, tests.
#
# RAG/LLM-agent-only deps (sentence-transformers, rank_bm25, faiss-cpu, peft,
# transformers, ollama client) are intentionally deferred to M7/M8 (Part 2) so
# this install stays fast and lean while they're unused. See CLAUDE.md Current
# Status for the note on this deviation.
--extra-index-url https://download.pytorch.org/whl/cpu

numpy==2.1.3
scipy==1.14.1
pandas==2.2.3
pyarrow==18.1.0
torch==2.5.1
scikit-learn==1.5.2
matplotlib==3.9.3
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pytest==8.3.4
